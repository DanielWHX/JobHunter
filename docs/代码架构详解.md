# JobHunter 代码架构详解

## 目录
1. [项目整体架构](#项目整体架构)
2. [文件结构详解](#文件结构详解)
3. [数据流动图](#数据流动图)
4. [组件协作关系](#组件协作关系)
5. [TypeScript 类型系统](#typescript-类型系统)
6. [LocalStorage 持久化机制](#localstorage-持久化机制)

---

## 项目整体架构

### 架构图

```
┌─────────────────────────────────────────┐
│           浏览器 (Browser)               │
│  ┌───────────────────────────────────┐  │
│  │     React 应用 (Single Page App)  │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │      App.tsx (主组件)       │  │  │
│  │  │  - 状态管理                  │  │  │
│  │  │  - 业务逻辑                  │  │  │
│  │  │  └─ useLocalStorage Hook    │  │  │
│  │  └─────────────┬───────────────┘  │  │
│  │                │                   │  │
│  │        ┌───────┴────────┐          │  │
│  │        │                │          │  │
│  │   ┌────▼─────┐    ┌────▼─────┐    │  │
│  │   │TaskForm  │    │TaskList  │    │  │
│  │   │(表单组件)│    │(列表组件)│    │  │
│  │   └──────────┘    └──────────┘    │  │
│  └───────────────────────────────────┘  │
│                  │                       │
│                  ▼                       │
│  ┌───────────────────────────────────┐  │
│  │   LocalStorage (浏览器本地存储)   │  │
│  │   Key: 'jobhunter-tasks'          │  │
│  │   Value: JSON 格式的任务数组       │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

### 技术栈层次

```
┌─────────────────────────────┐
│   UI 层 (Tailwind CSS)      │  样式
├─────────────────────────────┤
│   组件层 (React Components)  │  界面
├─────────────────────────────┤
│   状态层 (React Hooks)       │  数据管理
├─────────────────────────────┤
│   类型层 (TypeScript)        │  类型安全
├─────────────────────────────┤
│   持久化层 (LocalStorage)    | 数据存储
└─────────────────────────────┘
```

---

## 文件结构详解

### 完整目录树

```
frontend/src/
├── main.tsx                    # 📌 应用入口
├── App.tsx                     # 📌 主应用组件
├── index.css                   # 📌 全局样式
├── vite-env.d.ts              # TypeScript 环境声明
├── components/                 # 📌 UI 组件文件夹
│   ├── TaskForm.tsx           # 任务表单组件
│   └── TaskList.tsx           # 任务列表组件
├── hooks/                      # 📌 自定义 Hooks
│   └── useLocalStorage.ts     # LocalStorage 持久化 Hook
└── types/                      # 📌 类型定义文件夹
    └── task.ts                 # 任务相关类型定义
```

---

### 1. main.tsx - 应用入口

**作用**: React 应用的启动文件

```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'
import './index.css'

// 将 App 组件挂载到 HTML 的 #root 元素上
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

**职责**:
- ✅ 找到 HTML 中的 `<div id="root"></div>`
- ✅ 将 React 应用渲染到这个 DOM 节点
- ✅ 启用严格模式（帮助发现潜在问题）

**何时执行**: 页面加载时自动执行一次

---

### 2. App.tsx - 主应用组件

**作用**: 整个应用的"大脑"，管理所有状态和业务逻辑

```tsx
// 伪代码示意
function App() {
  // 📊 状态管理
  const [tasks, setTasks] = useLocalStorage('jobhunter-tasks', [])
  const [editingTask, setEditingTask] = useState(null)

  // 🔧 业务逻辑函数
  const handleCreateTask = (input) => { ... }
  const handleUpdateTask = (id, updates) => { ... }
  const handleDeleteTask = (id) => { ... }
  const handleToggleStatus = (id) => { ... }

  // 🎨 渲染界面
  return (
    <div>
      <TaskForm onSubmit={...} />
      <TaskList tasks={tasks} onToggle={...} />
    </div>
  )
}
```

**职责**:
- ✅ **状态管理**: 存储所有任务数据和编辑状态
- ✅ **业务逻辑**: 处理增删改查操作
- ✅ **组件协调**: 连接 TaskForm 和 TaskList
- ✅ **数据持久化**: 通过 useLocalStorage 自动保存

**数据流**:
```
用户操作 → TaskForm/TaskList 触发事件
  → App 中的处理函数更新 tasks
    → useLocalStorage 自动保存到浏览器
      → TaskList 重新渲染显示新数据
```

---

### 3. components/TaskForm.tsx - 任务表单组件

**作用**: 用户输入界面，用于创建和编辑任务

**内部结构**:
```tsx
function TaskForm({ onSubmit, initialData, onCancel }) {
  // 📝 表单字段状态
  const [title, setTitle] = useState('')
  const [description, setDescription] = useState('')
  const [category, setCategory] = useState(...)
  const [priority, setPriority] = useState(...)

  // 📤 提交处理
  const handleSubmit = (e) => {
    e.preventDefault()
    onSubmit({ title, description, category, priority })
  }

  // 🎨 表单 UI
  return <form>...</form>
}
```

**职责**:
- ✅ **收集用户输入**: 标题、描述、分类、优先级等
- ✅ **表单验证**: 检查必填项
- ✅ **数据提交**: 调用父组件传来的 `onSubmit` 函数
- ✅ **编辑模式**: 根据 `initialData` 预填充表单

**与 App.tsx 的交互**:
```
App.tsx 传递:
  - onSubmit 函数 (用于创建/更新任务)
  - initialData (编辑时的初始数据)
  - onCancel (取消编辑)

TaskForm 调用:
  - onSubmit({ 用户填写的数据 })
```

---

### 4. components/TaskList.tsx - 任务列表组件

**作用**: 显示所有任务，提供操作按钮

**内部结构**:
```tsx
function TaskList({ tasks, onToggleStatus, onEdit, onDelete }) {
  return (
    <div>
      {tasks.map(task => (
        <TaskItem
          key={task.id}
          task={task}
          onToggle={() => onToggleStatus(task.id)}
          onEdit={() => onEdit(task)}
          onDelete={() => onDelete(task.id)}
        />
      ))}
    </div>
  )
}
```

**职责**:
- ✅ **展示任务**: 遍历 tasks 数组渲染每个任务
- ✅ **用户交互**: 提供完成、编辑、删除按钮
- ✅ **状态显示**: 显示任务的完成状态、优先级等

**与 App.tsx 的交互**:
```
App.tsx 传递:
  - tasks 数组 (所有任务数据)
  - onToggleStatus (切换完成状态)
  - onEdit (开始编辑)
  - onDelete (删除任务)

TaskList 调用:
  - onToggleStatus(taskId)
  - onEdit(task)
  - onDelete(taskId)
```

---

### 5. hooks/useLocalStorage.ts - 持久化 Hook

**作用**: 自动将 React 状态同步到 LocalStorage

**工作原理**:
```tsx
export function useLocalStorage<T>(key: string, initialValue: T) {
  // 📖 阶段1: 初始化 - 从 LocalStorage 读取
  const [storedValue, setStoredValue] = useState<T>(() => {
    const item = localStorage.getItem(key)
    return item ? JSON.parse(item) : initialValue
  })

  // 💾 阶段2: 同步 - 每次状态变化时保存
  useEffect(() => {
    localStorage.setItem(key, JSON.stringify(storedValue))
  }, [key, storedValue])

  return [storedValue, setStoredValue]
}
```

**执行流程**:
```
1. 组件首次加载
   └─> 从 localStorage.getItem('jobhunter-tasks') 读取
   └─> 如果有数据，解析 JSON
   └─> 如果没有，使用 initialValue (空数组 [])

2. 用户创建任务
   └─> App.tsx 调用 setTasks([...tasks, newTask])
   └─> useLocalStorage 的 storedValue 更新
   └─> useEffect 检测到变化
   └─> 自动调用 localStorage.setItem('jobhunter-tasks', JSON.stringify(新数组))

3. 用户刷新页面
   └─> 回到步骤1，从 LocalStorage 读取之前保存的数据
```

**关键特性**:
- ✅ **自动保存**: 无需手动调用 localStorage API
- ✅ **日期处理**: 自动将日期字符串转换为 Date 对象
- ✅ **错误处理**: 如果 localStorage 失败，返回初始值

---

### 6. types/task.ts - 类型定义

**作用**: 定义应用中所有数据结构的"形状"

**核心类型**:

#### Task (任务完整数据)
```typescript
interface Task {
  id: string                    // 唯一标识符
  title: string                 // 标题 *必填
  description?: string          // 描述 (可选)
  category: TaskCategory        // 分类
  priority: TaskPriority        // 优先级
  status: TaskStatus            // 状态
  createdAt: Date              // 创建时间
  updatedAt: Date              // 更新时间
  completedAt?: Date           // 完成时间 (可选)
  estimatedMinutes?: number    // 预计时长 (可选)
}
```

#### 枚举类型
```typescript
// 优先级 (四象限法则)
enum TaskPriority {
  URGENT_IMPORTANT = 'urgent_important'           // A象限
  NOT_URGENT_IMPORTANT = 'not_urgent_important'   // B象限
  URGENT_NOT_IMPORTANT = 'urgent_not_important'   // C象限
  NOT_URGENT_NOT_IMPORTANT = 'not_urgent_not_important' // D象限
}

// 任务分类
enum TaskCategory {
  DAILY_LIFE = 'daily_life'      // 生活琐事
  SCHOOL = 'school'               // 学校事情
  JOB_LEARNING = 'job_learning'   // 找工学习
  // ... 等等
}
```

**为什么需要类型？**
```typescript
// ❌ 没有类型 - 容易出错
const task = {
  titel: "学习 React",  // 拼写错误！
  priority: "very high"  // 不符合规范！
}

// ✅ 有类型 - TypeScript 会报错
const task: Task = {
  title: "学习 React",   // 正确
  priority: TaskPriority.URGENT_IMPORTANT  // 正确
}
```

---

## 数据流动图

### 创建任务的完整流程

```
1. 用户在 TaskForm 输入数据
   │
   ├─ 标题: "学习 React Hooks"
   ├─ 分类: JOB_LEARNING
   └─ 优先级: NOT_URGENT_IMPORTANT
   │
2. 用户点击"创建任务"按钮
   │
   ▼
3. TaskForm.handleSubmit 被触发
   │
   ├─ 阻止表单默认提交: e.preventDefault()
   ├─ 组装数据对象: { title, category, priority, ... }
   └─ 调用: onSubmit(数据对象)
   │
   ▼
4. App.handleCreateTask 被调用
   │
   ├─ 生成新任务对象:
   │  {
   │    id: "1704153600000",        // 时间戳
   │    title: "学习 React Hooks",
   │    category: "job_learning",
   │    priority: "not_urgent_important",
   │    status: "pending",
   │    createdAt: new Date(),
   │    updatedAt: new Date()
   │  }
   │
   └─ 更新状态: setTasks([...tasks, newTask])
   │
   ▼
5. useLocalStorage 检测到 tasks 变化
   │
   ├─ 触发 useEffect
   └─ 保存到浏览器:
      localStorage.setItem('jobhunter-tasks', JSON.stringify(tasks))
   │
   ▼
6. React 重新渲染组件
   │
   ├─ App 组件重新渲染
   ├─ TaskList 接收新的 tasks 数组
   └─ 新任务显示在列表中
```

### 刷新页面后的数据恢复

```
1. 用户刷新页面
   │
   ▼
2. main.tsx 执行，挂载 App 组件
   │
   ▼
3. App 组件初始化
   │
   └─ 调用: useLocalStorage('jobhunter-tasks', [])
   │
   ▼
4. useLocalStorage 的 useState 初始化函数执行
   │
   ├─ 读取: localStorage.getItem('jobhunter-tasks')
   ├─ 解析 JSON: JSON.parse(item, 日期转换器)
   └─ 返回: 之前保存的任务数组
   │
   ▼
5. App 使用恢复的数据渲染界面
   │
   └─ TaskList 显示所有之前的任务
```

---

## 组件协作关系

### 父子组件通信

```
App (父组件)
│
├─ 向 TaskForm 传递:
│  ├─ onSubmit={handleCreateTask}      Props (属性)
│  ├─ initialData={editingTask}        ↓
│  └─ onCancel={() => setEditingTask(null)}
│
├─ 从 TaskForm 接收:
│  └─ onSubmit 被调用，传回用户输入的数据  ↑
│                                      回调函数
│
├─ 向 TaskList 传递:
│  ├─ tasks={tasks}                    Props
│  ├─ onToggleStatus={handleToggleStatus}  ↓
│  ├─ onEdit={setEditingTask}
│  └─ onDelete={handleDeleteTask}
│
└─ 从 TaskList 接收:
   └─ 各种回调函数被调用，传回任务ID或任务对象  ↑
                                        回调函数
```

### 状态提升 (State Lifting)

为什么 `tasks` 状态在 App 而不是 TaskList 中？

```
❌ 错误做法 - 状态分散
TaskForm (自己的状态)
  └─ 新任务如何传给 TaskList？❌

TaskList (自己的状态)
  └─ 如何接收新任务？❌

✅ 正确做法 - 状态提升
App (统一的状态)
  ├─ TaskForm ← 可以添加任务
  └─ TaskList ← 可以显示任务
```

---

## TypeScript 类型系统

### 类型检查的好处

```typescript
// 示例1: 防止拼写错误
function createTask(task: Task) {
  console.log(task.title)   // ✅ 正确
  console.log(task.titel)   // ❌ TypeScript 报错: 属性不存在
}

// 示例2: 防止类型错误
const tasks: Task[] = []
tasks.push({ title: "test" })  // ❌ 缺少必填字段
tasks.push("not a task")       // ❌ 类型不匹配

// 示例3: 自动补全
const task: Task = {
  id: "1",
  title: "学习",
  // 输入 task. 后，编辑器会自动提示所有可用属性
}
```

---

## LocalStorage 持久化机制

### LocalStorage 是什么？

浏览器提供的本地存储 API，数据永久保存在用户电脑上。

```javascript
// 原始 API (我们通过 Hook 封装了它)
localStorage.setItem('key', 'value')  // 保存
localStorage.getItem('key')           // 读取
localStorage.removeItem('key')        // 删除
```

### 数据存储位置

- **Windows**: `C:\Users\你的用户名\AppData\Local\Google\Chrome\User Data\Default\Local Storage`
- **Mac**: `~/Library/Application Support/Google/Chrome/Default/Local Storage`
- **浏览器开发者工具**: Application → Local Storage → http://localhost:5173

### 数据格式

```javascript
// 在 LocalStorage 中的实际存储格式
key: "jobhunter-tasks"
value: '[{"id":"1704153600000","title":"学习React","category":"job_learning","priority":"not_urgent_important","status":"pending","createdAt":"2024-01-02T03:00:00.000Z","updatedAt":"2024-01-02T03:00:00.000Z"}]'
```

---

## 总结

### 代码协作流程

```
1. main.tsx 启动应用
   ↓
2. App.tsx 初始化，从 LocalStorage 加载数据
   ↓
3. 用户在 TaskForm 创建任务
   ↓
4. TaskForm 触发 onSubmit，数据传回 App
   ↓
5. App 更新 tasks 状态
   ↓
6. useLocalStorage 自动保存到 LocalStorage
   ↓
7. React 重新渲染 TaskList
   ↓
8. 用户看到新任务
```

### 关键技术点

| 技术 | 作用 | 文件位置 |
|------|------|---------|
| React 组件 | UI 界面 | App.tsx, TaskForm.tsx, TaskList.tsx |
| React Hooks | 状态管理 | useState, useEffect, useLocalStorage |
| TypeScript | 类型安全 | types/task.ts |
| LocalStorage | 数据持久化 | hooks/useLocalStorage.ts |
| Tailwind CSS | 样式 | 组件中的 className |
| Vite | 开发服务器 | vite.config.ts |

---

## 下一步学习建议

1. 在浏览器开发者工具中查看 LocalStorage 数据
2. 在 App.tsx 中添加 `console.log(tasks)` 观察数据流动
3. 尝试修改 TaskForm，添加新字段
4. 阅读 React 官方文档关于 Hooks 的部分
