# 项目结构详解

## 📁 完整目录结构

```
frontend/
├── src/                      # 源代码目录
│   ├── components/          # React 组件
│   │   ├── TaskForm.tsx    # 任务表单组件
│   │   └── TaskList.tsx    # 任务列表组件
│   ├── types/              # TypeScript 类型定义
│   │   └── task.ts         # 任务相关的类型
│   ├── App.tsx             # 主应用组件(入口)
│   ├── main.tsx            # 应用启动文件
│   ├── index.css           # 全局 CSS 样式
│   └── vite-env.d.ts       # Vite 类型声明
├── public/                  # 静态资源(可选)
├── index.html              # HTML 入口文件
├── package.json            # 项目配置和依赖
├── tsconfig.json           # TypeScript 配置
├── vite.config.ts          # Vite 配置
├── tailwind.config.js      # TailwindCSS 配置
└── postcss.config.js       # PostCSS 配置
```

## 📄 核心文件详解

### 1. index.html - HTML 入口

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>JobHunter - 每日任务管理</title>
  </head>
  <body>
    <div id="root"></div>                  <!-- React 挂载点 -->
    <script type="module" src="/src/main.tsx"></script>  <!-- 入口脚本 -->
  </body>
</html>
```

**关键点**:
- `<div id="root">` - React 应用会渲染到这里
- `type="module"` - 使用 ES6 模块系统
- 指向 `main.tsx` - TypeScript + JSX 文件

### 2. src/main.tsx - 应用启动文件

```tsx
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

**作用**:
1. 找到 HTML 中的 `root` 元素
2. 创建 React 根节点
3. 渲染 `<App />` 组件
4. `StrictMode` 开启额外的检查(只在开发模式)

### 3. src/App.tsx - 主应用组件

这是应用的核心,包含:
- **状态管理**: 使用 `useState` 管理任务列表
- **事件处理**: 创建、更新、删除任务的函数
- **布局**: 左侧表单 + 右侧列表

```tsx
function App() {
  const [tasks, setTasks] = useState<Task[]>([]);  // 任务列表
  const [editingTask, setEditingTask] = useState<Task | null>(null);  // 正在编辑的任务

  // ... 各种处理函数

  return (
    <div>
      {/* 布局和组件 */}
    </div>
  );
}
```

### 4. src/types/task.ts - 类型定义

定义了所有任务相关的类型:

```typescript
// 枚举类型 - 固定的选项
export enum TaskPriority {
  URGENT_IMPORTANT = 'urgent_important',
  // ...
}

// 接口类型 - 描述对象的结构
export interface Task {
  id: string;
  title: string;
  // ...
}

// 标签映射 - 用于显示中文
export const PRIORITY_LABELS: Record<TaskPriority, string> = {
  // ...
}
```

**为什么需要类型?**
- 防止拼写错误
- 自动补全提示
- 重构时更安全

## 🔧 配置文件详解

### vite.config.ts

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],                    // 支持 React
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),  // '@' 指向 'src'
    },
  },
  server: {
    port: 3000,                          // 开发服务器端口
  },
})
```

**作用**:
- 配置 React 插件
- 设置路径别名(`@` = `src`)
- 自定义端口

### tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ES2017",           // 编译目标
    "module": "esnext",           // 模块系统
    "jsx": "preserve",            // JSX 处理方式
    "strict": true,               // 严格模式
    "paths": {
      "@/*": ["./*"]              // 路径映射
    }
  }
}
```

### tailwind.config.js

```javascript
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",  // 扫描这些文件中的类名
  ],
  theme: {
    extend: {},                     // 自定义主题
  },
  plugins: [],                      // TailwindCSS 插件
}
```

## 📦 组件结构

### TaskForm 组件

**职责**: 处理任务的创建和编辑

```
TaskForm.tsx
├── 状态管理(useState)
│   ├── title - 任务标题
│   ├── description - 任务描述
│   ├── category - 任务分类
│   ├── priority - 优先级
│   └── estimatedMinutes - 预计时长
├── 表单处理(handleSubmit)
└── 界面渲染(JSX)
    ├── 输入框
    ├── 下拉菜单
    └── 按钮
```

### TaskList 组件

**职责**: 展示和管理任务列表

```
TaskList.tsx
├── 数据处理
│   └── groupedTasks - 按优先级分组
├── 工具函数
│   ├── getPriorityColor - 获取优先级颜色
│   └── getCategoryColor - 获取分类颜色
└── 界面渲染
    ├── 统计卡片
    ├── 任务分组显示
    └── 任务卡片(renderTaskCard)
```

## 🎨 样式系统

### TailwindCSS 类名结构

```tsx
<div className="bg-white dark:bg-gray-800 rounded-lg shadow-lg p-6">
  {/*
    bg-white - 白色背景
    dark:bg-gray-800 - 深色模式下的灰色背景
    rounded-lg - 大圆角
    shadow-lg - 大阴影
    p-6 - 内边距 1.5rem
  */}
</div>
```

### 常用类名速查

| 功能 | 类名 | 说明 |
|------|------|------|
| 布局 | `flex`, `grid` | 弹性布局,网格布局 |
| 间距 | `p-4`, `m-2`, `gap-6` | 内边距,外边距,间隙 |
| 颜色 | `bg-blue-500`, `text-white` | 背景色,文字色 |
| 圆角 | `rounded`, `rounded-lg` | 圆角,大圆角 |
| 阴影 | `shadow`, `shadow-lg` | 阴影,大阴影 |
| 响应式 | `md:col-span-2`, `lg:grid-cols-3` | 中屏,大屏 |

## 📊 数据流

```
用户操作
    ↓
事件触发(onClick, onChange)
    ↓
App.tsx 中的处理函数
    ↓
更新 state (setTasks)
    ↓
React 重新渲染
    ↓
界面更新
```

**示例流程**: 创建新任务

1. 用户在 `TaskForm` 中填写表单
2. 点击"创建任务"按钮
3. 触发 `handleCreateTask` 函数
4. 创建新的 Task 对象
5. 更新 `tasks` 状态
6. `TaskList` 自动重新渲染
7. 新任务显示在列表中

## 下一步

- [核心概念](./03-核心概念.md) - 深入理解 React 和 TypeScript
- [组件开发指南](./04-组件开发.md) - 如何创建新组件
